{
  "name": "eventora",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently --names \"CLIENT,SERVER\" --prefix-colors \"cyan,magenta\" \"npm run client\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
